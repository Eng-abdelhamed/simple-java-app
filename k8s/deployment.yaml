apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-app
  labels:
    app: java-app
spec:
  selector:
    matchLabels:
        app: java-app
  replicas: _{_REPLICAS_}_
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: java-app
    spec:
      containers:
        - name: java-app
          image: _{_IMAGE_}_
          ports:
            - containerPort: 80
              protocol: TCP
          imagePullPolicy: Always
